"""Settings package — does NOT auto-import any environment.

Set DJANGO_SETTINGS_MODULE to the desired environment:
  - config.settings.development   (local dev, MySQL)
  - config.settings.vercel        (production, PostgreSQL)
  - config.settings.testing       (CI, SQLite)
"""
